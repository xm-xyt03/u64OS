#ifndef CONFIG_H
#define CONFIG_H

/*
 * KTEST_ENABLE
 *
 * Define this macro to run the built-in kernel test suite on boot.
 * Comment it out (or remove it) to skip all tests and reduce startup overhead.
 */
#define KTEST_ENABLE

#endif // !CONFIG_H
